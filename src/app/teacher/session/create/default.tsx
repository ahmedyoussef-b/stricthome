// src/app/teacher/session/create/default.tsx

// This file is required by Next.js when using parallel routes.
// It provides a fallback UI for a slot that doesn't match the current URL.
// In our case, we want to render nothing, so we return null.
// Learn more: https://nextjs.org/docs/app/building-your-application/routing/parallel-routes#defaultjs
export default function Default() {
  return null;
}
