// src/app/teacher/session/create/page.tsx

export default function CreateSessionPage() {
  // This page is not meant to be rendered directly.
  // It's a route segment for the session creation logic.
  // The user should be on the class page to start a session.
  return null;
}
